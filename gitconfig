[core]
    autocrlf = false
    safecrlf = true
    editor = vim
[user]
    name = Jefferson Wililam Chen
    email = jchen@artofproblemsolving.com
[init]
    defaultBranch = main
[alias]
    co = checkout
    ci = commit
    st = status
    br = branch
    bh = log --oneline --abbrev-commit --all --graph --decorate --color
    bhs = log stable.. --oneline --abbrev-commit --all --graph --decorate --color
    type = cat-file -t
    dump = cat-file -p
    files = ls-tree -r master --name-only
[pull]
	rebase = false
